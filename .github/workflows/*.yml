name: TDS GitHub Action

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: 23f2000576@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Show date
        run: date
